const emailRequiredField = 'Email is required field';
const firstNameRequiredField = 'First Name is required field';
const lastNameRequiredField = 'Last Name is required field';
const companyNameRequiredField = 'Company Name is required field';
const passwordRequiredField = 'Password is required field';
const confirmPasswordRequiredField =
  'Confirmation of you password is required field';

const firstNameInvalidFormat = 'Invalid first name format';
const lastNameInvalidFormat = 'Invalid last name format';
const companyNameInvalidFormat = 'Invalid company name format';
const emailInvalidFormat = 'Invalid email format';
const companyWebsiteInvalidFormat = 'Invalid company website format';
const passwordsMatchRequiredField = 'Passwords don`t match';
const passwordErrorMessage =
  'Password should contain at least: 1 special character, 1 number, 1 uppercase, 1 lowercase';

const emailMinMessage = 'Email should contain at least 2 characters';
const firstNameMinMessage = 'First Name should contain at least 2 characters';
const lastNameMinMessage = 'Last Name should contain at least 2 characters';
const companyNameMinMessage =
  'Company Name should contain at least 2 characters';
const passwordMinMessage = 'Password should contain at least 8 characters';

const emailMaxMessage = 'Email should`t be more than 128 characters';
const firstNameMaxMessage = 'First Name should`t be more than 128 characters';
const lastNameMaxMessage = 'Last Name should`t be more than 128 characters';
const companyNameMaxMessage =
  'Company Name should`t be more than 128 characters';
const passwordMaxMessage = 'Password should`t be more than 64 characters';

// Generate Code
const linkRequiredField = 'Insert link field is the required field';
const linkInvalidFormat = 'Invalid format';
const purposeRequiredField = 'Purpose field is the required field';
const purposeMaxMessage = 'Purpose should`t be more then 256 characters';
const purposeMinMessage = 'Purpose should contain at least 2 characters';

const defaultErrorText = 'Something went wrong. Please try again later.';

export {
  emailRequiredField,
  firstNameRequiredField,
  lastNameRequiredField,
  companyNameRequiredField,
  passwordRequiredField,
  confirmPasswordRequiredField,
  emailInvalidFormat,
  passwordsMatchRequiredField,
  passwordErrorMessage,
  emailMinMessage,
  firstNameMinMessage,
  lastNameMinMessage,
  companyNameMinMessage,
  passwordMinMessage,
  emailMaxMessage,
  firstNameMaxMessage,
  lastNameMaxMessage,
  companyNameMaxMessage,
  passwordMaxMessage,
  firstNameInvalidFormat,
  lastNameInvalidFormat,
  companyNameInvalidFormat,
  linkRequiredField,
  purposeRequiredField,
  purposeMaxMessage,
  purposeMinMessage,
  linkInvalidFormat,
  companyWebsiteInvalidFormat,
  defaultErrorText,
};
